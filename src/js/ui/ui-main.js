/**
 * UI MAIN
 * ==================
 * This file creates the global UI object, which serves as a namespace for all
 * other UI modules to attach their components. It must be the first UI
 * script loaded.
 */

const UI = {};